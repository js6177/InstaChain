[project]
name = "layer1-utils"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = []
requires-python = ">=3.12"
dependencies = [
    "bip-utils>=2.10.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
